Database Data:
               database3 in AWS. Make sure to turn it off when not in use in AWS to avoid extra charges.

               execute and goto to see available services. This is a backend project. Not intended to have a frontend.
               localhost:8080/